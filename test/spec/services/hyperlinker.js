'use strict';

describe('Service: hyperLinker', function () {

  // load the service's module
  beforeEach(module('schemajsonApp'));

  // instantiate service
  var hyperLinker;
  beforeEach(inject(function (_hyperLinker_) {
    hyperLinker = _hyperLinker_;
  }));

  it('should perform a GET', function () {
//    expect(!!hyperLinker).toBe(true);
//    hyperLinker.traverse()
  });

});
